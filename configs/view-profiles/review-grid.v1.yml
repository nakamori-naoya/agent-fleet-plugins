apiVersion: fleet.herdr.harness/v1
kind: ViewProfile
metadata:
  id: local/review-grid
  version: 1
spec:
  constraints:
    min_members: 2
    max_members: 5
  layout:
    type: split
    direction: horizontal
    children:
      - type: slot
        selector: manager
        weight: 42
      - type: stack
        selector: non-manager
        weight: 58
        direction: vertical
        distribution: equal
