{
  "description": "Agent Fleetの役割文脈をClaude Code fleet sessionへ再注入する",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh",
            "args": [
              "-c",
              "if [ -n \"${AGENT_FLEET_HOOK_RUNTIME:-}\" ]; then exec python3 \"$AGENT_FLEET_HOOK_RUNTIME\" --runtime-product claude; fi"
            ],
            "timeout": 12,
            "statusMessage": "艦隊の役割文脈を確認しています"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact|fork",
        "hooks": [
          {
            "type": "command",
            "command": "sh",
            "args": [
              "-c",
              "if [ -n \"${AGENT_FLEET_HOOK_RUNTIME:-}\" ]; then exec python3 \"$AGENT_FLEET_HOOK_RUNTIME\" --runtime-product claude; fi"
            ],
            "timeout": 12,
            "statusMessage": "艦隊の役割文脈を復元しています"
          }
        ]
      }
    ]
  }
}
