---
name: fleet-control
description: Agent Fleetのローカル状態、task遷移、event、typed command outboxをSQLiteで操作する。
---

# Fleet control

詳細な契約とCLIは `${CLAUDE_PLUGIN_ROOT}/core/SKILL.md` を読み、`${CLAUDE_PLUGIN_ROOT}/core/scripts/fleet-control` を使う。Herdrのpane IDをCoreへ書き込まない。
