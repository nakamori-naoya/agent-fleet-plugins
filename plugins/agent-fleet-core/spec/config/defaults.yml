apiVersion: fleet.harness/v1
kind: FleetDefaults
metadata:
  id: mvp
spec:
  collaboration:
    reporting:
      strategy: manager
      include_task_updates: true
  runtime:
    provider: herdr
  view:
    profile_ref: builtin/command-deck@1
